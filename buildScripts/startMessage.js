/* eslint-disable no-console */
//var chalk = require("chalk"); // enables you to change text color
import chalk from 'chalk'; // ES6 syntax

console.log(chalk.green("Hi there, let's start the development webserver...."));
